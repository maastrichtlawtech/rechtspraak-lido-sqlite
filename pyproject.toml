[project]
name = "rechtspraak-self-host"
version = "0.1.0"
description = "Build a local SQLite database from the lido-export.ttl.gz linked-data export for use with rechtspraak-extractor"
requires-python = ">=3.10"
dependencies = [
    "pyoxigraph>=0.3",
    "tqdm",
    "requests",
]
